{
  "name": "timer",
  "version": "1.0.0",
  "description": "A simple multi-timer",
  "main": "index.js",
  "scripts": {
    "postinstall": "true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "google-closure-compiler": "^20170409.0.0",
    "gulp": "^4.0.2"
  }
}
